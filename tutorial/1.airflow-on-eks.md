```
export CLUSTER_NAME="training-on-eks"
```


### 1. 단계: IAM 및 Namespace 준비 ###
먼저 Airflow가 설치될 공간과 S3 로그 저장 등을 위한 IAM 역할을 생성합니다. (eksctl 사용)
```
kubectl create namespace airflow

# S3 접근 권한을 가진 ServiceAccount 생성 (IRSA)
eksctl create iamserviceaccount \
  --name airflow-sa \
  --namespace airflow \
  --cluster ${CLUSTER_NAME}\
  --role-name AirflowEKSRole \
  --attach-policy-arn arn:aws:iam::aws:policy/AmazonS3FullAccess \
  --approve
```

### 2. 단계: Git 접속 정보(SSH Key) 설정 ###
Private 리포지토리를 사용한다면, Airflow가 Git에 접속할 수 있도록 SSH Key를 쿠버네티스 시크릿으로 등록해야 합니다.

```
# 1. SSH 키 생성 (이미 있다면 건너뜀)
ssh-keygen -t rsa -b 4096 -f ./airflow-git-key

# 2. 생성된 public key(.pub)를 GitHub/GitLab의 'Deploy Keys'에 등록

# 3. Private Key를 쿠버네티스 Secret으로 생성
kubectl create secret generic airflow-git-ssh-secret \
  --from-file=gitSshKey=./airflow-git-key \
  -n airflow

kubectl get secret airflow-git-ssh-secret -n airflow -o yaml
```

### 3. s3 버킷 생성 ###
```
export CLUSTER_NAME="training-on-eks"
export AWS_REGION=$(aws ec2 describe-availability-zones --query "AvailabilityZones[0].RegionName" --output text)
export ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)

S3_BUCKET=$(aws s3api create-bucket --bucket mlops-on-eks-${ACCOUNT_ID} --region ${AWS_REGION} \
  --create-bucket-configuration LocationConstraint=${AWS_REGION} --query "Location" --output text)

echo ${S3_BUCKET}" created ..."
```

### 4. 단계: values.yaml 작성 ###
Git-Sync 활성화와 외부 서비스 연결을 포함한 설정 파일입니다.
```
cat <<'EOF' > values.yaml
executor: "KubernetesExecutor"

serviceAccount:
  create: false
  name: "airflow-sa"

# ---------------------------------------------------------
# Git-Sync 설정 (S3 대신 Git 리포지토리에서 DAG 로드)
# ---------------------------------------------------------
dags:
  gitSync:
    enabled: true
    repo: "git@github.com:gnosia93/mlops-on-eks.git"             # SSH 주소
    branch: "main"
    rev: "HEAD"
    depth: 1
    wait: 60  # 60초마다 동기화 체크
    subPath: "dags"                                              # 리포지토리 내 DAG 파일이 있는 특정 폴더 경로
    sshKeySecret: "airflow-git-ssh-secret"                       # 2단계에서 만든 시크릿 이름

# ---------------------------------------------------------
# 외부 DB(RDS) 및 S3 로그 설정 (운영 필수)
# ---------------------------------------------------------
data:
  metadataConnection:
    user: "postgres"
    pass: "your-password"
    protocol: "postgresql"
    host: "your-rds-endpoint.aws.com"
    port: 5432
    db: "airflow"

config:
  logging:
    remote_logging: "True"
    remote_base_log_folder: "s3://${S3_BUCKET}"
    remote_log_conn_id: "aws_default"
EOF 

helm repo add apache-airflow https://airflow.apache.org
helm repo update
helm install airflow apache-airflow/airflow --namespace airflow \
  --values values.yaml
```

### 5. 단계: LLM 파이프라인 개발 워크플로우 ###
이제 설치가 끝났습니다. 앞으로 LLM 파인튜닝 자동화는 다음과 같은 순서로 진행하면 됩니다.
* 코드 수정: 로컬 IDE에서 DAG 파일(예: llm_finetune_dag.py)을 작성합니다.
* Git Push: git push origin main으로 코드를 올립니다.
* 자동 반영: EKS에 떠 있는 Airflow Git-Sync 컨테이너가 60초 이내에 새 코드를 감지하여 웹 UI에 표시합니다.
* 학습 실행: DAG가 실행되면 KubernetesPodOperator가 GPU 노드에 학습용 Pod를 띄워 파인튜닝을 시작합니다.

### 💡 운영 팁 (2025) ###
* 파일 구조: Git 리포지토리 루트에 dags/ 폴더를 만들고 그 안에 파일을 넣으세요.
* 보안: RDS 비밀번호 등은 values.yaml에 직접 적지 말고 쿠버네티스 Secret을 연동하여 관리하는 것이 안전합니다.
* GPU 노드: EKS 노드 그룹에 GPU 인스턴스(P계열/G계열)를 추가하고, DAG 코드 내에서 nodeSelector: { "nvidia.com": "exists" }를 설정해 학습 작업을 해당 노드로 보내야 합니다.


